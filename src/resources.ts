export const RESOURCES: Record<string, { title: string; body: string }> = {
  "alpha-report": {
    title: "Base flow report",
    body: "Demo payload unlocked after USDC settlement on Base.",
  },
};
